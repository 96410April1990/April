package week7;

public class ListOfMethods {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		
	
		/**
		 * List of methods
		 * 
		 * length() - returns the number of characters including the white spaces	
		 * charAt(index) - to find out where the particular character is
		 * concat() - to concatenate the strings
		 * toString() - convert any data type to string
		 * subString(beingIndex, endIndex)
		 * trim - removed the white spaces in the beginning and in the end
		 * toLowerCase, toUpperCase
		 * split(regex, limit) - To split a character into array
		 * indexOf(chr, startIndex), lastIndexOf(chr, StartIndex) - returns the position of the char
		 * startsWith(s), endsWith(s), contains(s) - returns boolean
		 * replace(oldChar, newChar), replaceAll(regex, replacement)
		 * Immutable String, StringBuffer
		 * 
		 */

	}

}
